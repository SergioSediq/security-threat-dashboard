# Copy to .env for local overrides (see app/config.py).
USE_MOCK_DATA=true
# Comma-separated browser origins for CORS (defaults are fine for local dev).
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:8080,http://127.0.0.1:8080
# Optional; live NVD integration is not wired in this demo build.
# NVD_API_KEY=
